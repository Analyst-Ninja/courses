[project]
name = "learn-pytest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=25.1.0",
    "isort>=6.0.1",
    "pytest>=8.4.0",
    "pytest-cov>=6.1.1",
    "requests>=2.32.3",
]
